#include <stdio.h>
#include <cs50.h>

int main(void)
{
    int first_number = get_int("write the first number :\n");
    int secund_number = ger_int ("wirte the secound number :\n");

    if (first_number > secund_number)
    {
        ptintf("%s is biger then %s", first_number , secund_number);
    }
    else if()
}
