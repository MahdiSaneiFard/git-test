#include <stdio.h>

;
int main(void)
{
    int b;
    int max;
    int mall;
    int a;
    scanf("%d%d", &a,&b);
    if(a < b)
    {
        a = mall;
        b = max
    }
    else
    {
        b = mall;
        a = max;
    }
    for(int i = max; i >= 1; i--)
    {
        if( == 0)
        {



        }
    }
}
