#include <stdio.h>

;
int main(void)
{
    int b;
    int max;
    int mall;
    int a;
    scanf("%d%d", &a,&b);
    if(a < b)
    {
        a = mall;
    }
    else
    b = mall;
    for(int i = 1; i <= a; i++)
    {
        if(a%i == 0)
        {
            


        }
    }
}
